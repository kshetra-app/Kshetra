# DEFECT REGISTER: PANIN / KSHETRA
**Last Updated:** 2026-09-08
**Classification:** P0 (Production Blocker), P1 (Major / Architectural), P2 (Moderate / Debt), P3 (Minor / Polish)

---

| Defect ID | Severity | Domain | Description | Reproduction / Evidence | Root Cause | Proposed Fix | Status |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **DEF-001** | **P1** | Mobile / Routing | Duplicate route files in `apps/mobile/app/` | `app/user/[id].tsx` vs `app/user/[userId].tsx`<br>`app/edit-profile.tsx` vs `app/auth/edit-profile.tsx`<br>`app/onboarding.tsx` vs `app/auth/onboarding.tsx` | Incomplete refactoring during auth module separation | Consolidate onto canonical routes and remove duplicate files | OPEN |
| **DEF-002** | **P1** | Mobile / Data | Deceptive local success fallbacks when backend offline | `apps/mobile/lib/supabaseDataService.ts` lines 62, 77, 93 (`if (!guard()) return true;`) | Legacy mock behavior masquerading as successful persistence | Replace with explicit `{ status: 'offline_queued' }` or honest error propagation | OPEN |
| **DEF-003** | **P1** | Mobile / Binary Size | `react-native-webrtc` bundled in consumer application | `apps/mobile/package.json` line 61 | Creator broadcasting infrastructure bundled into consumer app | Remove WebRTC from consumer package; decouple to web receiver / studio | OPEN |
| **DEF-004** | **P1** | Backend / Trust | Silent moderation check bypass on network failure | `apps/mobile/lib/supabaseDataService.ts` line 44 (`console.warn(...); return { flagged: false };`) | Permissive catch block in pre-submission moderation | Establish formal async queue vs sync block policy per content risk class | OPEN |
| **DEF-005** | **P2** | Architecture | Ambiguous dual-backend calling convention | 12 files call Supabase JS; 8 files call Railway Fastify; 15 files use local fallbacks | Dual infrastructure without a canonical API gateway client | Implement Canonical API Client (W007) and standardize domain routes | OPEN |
| **DEF-006** | **P2** | Database / GIS | Missing versioned geography tables specified in Master Blueprint | Supabase migrations currently use flat tables (`constituencies`, `mandals`) rather than versioned graph | Historical boundary preservation not yet modeled in schema | Add `geography_entity`, `geography_relationship`, `geography_version` migrations | OPEN |
| **DEF-007** | **P2** | Mobile / Shorts | Synthetic UUID check causing diverging identifier semantics | `apps/mobile/lib/supabaseDataService.ts` lines 1557, 1587 | Short IDs checked for UUID format; non-UUIDs silently return mock success | Standardize UUID generation across all client/server content touchpoints | OPEN |
| **DEF-008** | **P2** | Mobile / i18n | Residual hardcoded strings across screens bypassing 13-language translations | Dynamic screens (`app/delimitation/*`, `app/issue/*`, `components/*`) with inline English strings | Rapid prototyping without strict `t()` wrapping | Perform comprehensive static scan for unlocalized strings and backfill keys into all 13 locales | OPEN |
| **DEF-009** | **P1** | Backend / Security | Rejection of modern `sb_secret_...` Supabase service-role format | `apps/api/src/lib/supabase.ts` previously enforced `isJwt` (3 dots), erroneously rejecting modern Supabase `sb_secret_...` API keys and falling back to anon | `isJwt` check only accommodated legacy JWTs | Updated `isValidSupabaseKey` to accept both `sb_secret_...` and legacy 3-part JWTs; removed production warning gate; verified via unit tests | RESOLVED IN CODE |
| **DEF-010** | **P2** | DevOps / CORS | Missing `CORS_ORIGINS` on Railway production deployment | Live HTTP check with `Origin: https://kshetra.in` returned undefined header | `server.ts` defaulted to `return false` in production when `CORS_ORIGINS` unset | Added `DEFAULT_ALLOWED_ORIGINS` (`kshetra.in`, `www.kshetra.in`, `panin.in`, `www.panin.in`, `localhost:8081`). Committed code passes inject tests. Railway container requires deployment refresh. | RESOLVED IN CODE / PENDING DEPLOYMENT |
| **DEF-011** | **P2** | Database / Civic | Table `public.issue_categories` missing from production Supabase schema | Diagnostic probe queried `public.issue_categories` returning 404 (`PGRST205`) | Schema intentionally models categories via enum CHECK constraint on `public.civic_issues` (`004_civic_dashboard.sql`). Code uses `IssueCategory` union. No `issue_categories` table exists by design. | Verified `civic_issues` table exists and returns 200 OK; probe adjusted to target `civic_issues`. | CLOSED (INVALID DEFECT) |
| **DEF-012** | **P2** | Mobile / i18n | 13-language translation key parity gap (8 of 13 languages at ~56% coverage) | `scripts/verify-13-locales.mjs --strict` fails with exit code 1. English: 2,041 keys; `te` (90%), `hi` (86%), `kn`/`mr` (82%), `ta`/`ml`/`bn`/`gu`/`or`/`pa`/`as`/`ne` (56%, 904 missing keys). | Feature additions in `en.ts` not backpropagated to other 12 locale dictionaries; reliant on English fallback at runtime. | Canonical validator requires 100% key parity without threshold leniency. All 904 missing keys must be translated across all 12 Indic locales before production release. | OPEN |
| **DEF-013** | **P2** | Database / SQL | Invalid UNION ORDER BY clause in `global_search` RPC function | `supabase/migrations/020_foundation_hardening.sql` line 643; Live Staging probe returns HTTP 400 with PostgreSQL error `0A000: invalid UNION/INTERSECT/EXCEPT ORDER BY clause` | In PostgreSQL, UNION ALL chain cannot have trailing ORDER BY without wrapping query or matching select column alias | Wrap UNION ALL subqueries in `SELECT * FROM (...) search_results ORDER BY relevance DESC LIMIT p_limit` via future migration (W007+ or dedicated DB patch) | OPEN |


